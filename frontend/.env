# Colour theme for the app. `taltech` is registered in src/theme/taltech.ts;
# the @helex/ui built-ins (helex, tedi, matrix) work here too. No secrets in
# this file — it is committed on purpose.
VITE_THEME=taltech
