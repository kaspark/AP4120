# GEMINI.md — ITE4120 template

Read [`AGENTS.md`](AGENTS.md) at the repo root. That is the contract for every
AI coding agent working here, Gemini Code Assist included; this file only points
there, and `AGENTS.md` wins on any conflict.

Keep in working context while you read it: spec first, tests second, never
commit to `main`, never edit a released Liquibase changeset, and run the
verification block at the end of `AGENTS.md` before any PR.
